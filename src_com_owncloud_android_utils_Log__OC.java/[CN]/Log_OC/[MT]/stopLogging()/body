{
  if (logFile != null) {
    isEnabled=false;
    try {
      buf=new BufferedWriter(new FileWriter(logFile,false));
      buf.append("");
      buf.close();
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
  }
}
