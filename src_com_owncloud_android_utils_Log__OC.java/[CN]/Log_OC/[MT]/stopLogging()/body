{
  if (mLogFile != null) {
    try {
      mBuf=new BufferedWriter(new FileWriter(mLogFile,false));
      mBuf.append("");
      mBuf.close();
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
  }
}
