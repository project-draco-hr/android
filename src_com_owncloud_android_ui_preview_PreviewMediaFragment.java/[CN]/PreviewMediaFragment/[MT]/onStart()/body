{
  super.onStart();
  Log_OC.v(TAG,"onStart");
  OCFile file=getFile();
  if (file != null && file.isDown()) {
    if (file.isAudio()) {
      bindMediaService();
    }
 else {
      if (file.isVideo()) {
        stopAudio();
        playVideo();
      }
    }
  }
}
