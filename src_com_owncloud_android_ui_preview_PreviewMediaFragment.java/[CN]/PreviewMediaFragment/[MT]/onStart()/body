{
  super.onStart();
  Log_OC.e(TAG,"onStart");
  OCFile file=getFile();
  if (file != null) {
    if (file.isAudio()) {
      bindMediaService();
    }
 else     if (file.isVideo()) {
      stopAudio();
      playVideo();
    }
  }
}
