{
  super.onStart();
  Log_OC.e(TAG,"onStart");
  if (mFile != null) {
    if (mFile.isAudio()) {
      bindMediaService();
    }
 else     if (mFile.isVideo()) {
      stopAudio();
      playVideo();
    }
  }
}
