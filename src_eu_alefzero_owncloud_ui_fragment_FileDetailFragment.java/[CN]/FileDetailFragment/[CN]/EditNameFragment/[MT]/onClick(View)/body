{
switch (view.getId()) {
case R.id.ok:
{
      mNewFilename=((TextView)getView().findViewById(R.id.user_input)).getText().toString();
      mResult=true;
    }
case R.id.cancel:
{
    dismiss();
    mListener.onDismiss(this);
  }
}
}
