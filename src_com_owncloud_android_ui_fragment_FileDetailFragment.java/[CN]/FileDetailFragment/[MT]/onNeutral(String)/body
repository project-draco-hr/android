{
  OCFile file=getFile();
  ((FileActivity)getActivity()).getStorageManager().removeFile(file,false,true);
  if (file.getStoragePath() != null) {
    file.setStoragePath(null);
    updateFileDetails(file,mAccount);
  }
}
