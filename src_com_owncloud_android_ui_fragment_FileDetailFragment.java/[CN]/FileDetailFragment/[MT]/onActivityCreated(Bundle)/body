{
  super.onActivityCreated(savedInstanceState);
  if (mAccount != null) {
    OCFile file=((FileActivity)getActivity()).getStorageManager().getFileByPath(getFile().getRemotePath());
    if (file != null) {
      setFile(file);
    }
  }
}
