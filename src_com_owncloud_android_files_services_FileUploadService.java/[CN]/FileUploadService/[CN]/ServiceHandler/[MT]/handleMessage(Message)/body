{
  @SuppressWarnings("unchecked") AbstractList<UploadDbObject> requestedUploads=(AbstractList<UploadDbObject>)msg.obj;
  if (msg.obj != null) {
    Iterator<UploadDbObject> it=requestedUploads.iterator();
    while (it.hasNext()) {
      UploadDbObject uploadObject=it.next();
      mService.uploadFile(uploadObject);
    }
  }
  mService.stopSelf(msg.arg1);
}
