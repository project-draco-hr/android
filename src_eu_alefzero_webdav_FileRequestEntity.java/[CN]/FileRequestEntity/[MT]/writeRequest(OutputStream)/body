{
  byte[] tmp=new byte[4096];
  int i=0;
  InputStream instream=new FileInputStream(this.file);
  try {
    while ((i=instream.read(tmp)) >= 0) {
      out.write(tmp,0,i);
      if (listener != null)       listener.OnUploadProgress(i);
    }
  }
  finally {
    instream.close();
  }
}
