{
  int targetPort=mTargetHost.getPort() == -1 ? (mUri.getScheme().equals("https") ? 443 : 80) : mUri.getPort();
  getParams().setAuthenticationPreemptive(true);
  getState().setCredentials(AuthScope.ANY,getCredentials(username,password));
}
