{
  if (isGroup) {
    Log_OC.d(TAG,"You want to SHARE with GROUP [" + shareeName + "]");
  }
 else {
    Log_OC.d(TAG,"You want to SHARE with USER [" + shareeName + "]");
  }
  getFileOperationsHelper().shareFileWithSharee(getFile(),shareeName,(isGroup ? ShareType.GROUP : ShareType.USER));
}
