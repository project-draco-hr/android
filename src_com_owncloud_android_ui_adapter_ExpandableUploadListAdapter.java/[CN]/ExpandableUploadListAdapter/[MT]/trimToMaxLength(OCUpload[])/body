{
  if (items.length > 30) {
    OCUpload[] arrayTrim=new OCUpload[30];
    for (int i=0; i < MAX_NUM_UPLOADS_SHOWN; i++) {
      arrayTrim[i]=items[i];
    }
    return arrayTrim;
  }
 else {
    return items;
  }
}
