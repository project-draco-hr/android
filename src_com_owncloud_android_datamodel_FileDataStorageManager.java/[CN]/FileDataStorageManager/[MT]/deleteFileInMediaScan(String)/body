{
  String mimetypeString=FileStorageUtils.getMimeTypeFromName(path);
  if (mimetypeString.startsWith("image/")) {
    getContentResolver().delete(MediaStore.Images.Media.EXTERNAL_CONTENT_URI,MediaStore.Images.Media.DATA + "=?",new String[]{path});
  }
 else   if (mimetypeString.startsWith("audio/")) {
    getContentResolver().delete(MediaStore.Audio.Media.EXTERNAL_CONTENT_URI,MediaStore.Audio.Media.DATA + "=?",new String[]{path});
  }
 else   if (mimetypeString.startsWith("video/")) {
    getContentResolver().delete(MediaStore.Video.Media.EXTERNAL_CONTENT_URI,MediaStore.Video.Media.DATA + "=?",new String[]{path});
  }
}
