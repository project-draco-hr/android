{
  if (!mServerInfo.mBaseUrl.equals(normalizeUrl(mHostUrlInput.getText().toString(),mServerInfo.mIsSslConn))) {
    checkOcServer();
  }
 else {
    mOkButton.setEnabled(mServerIsValid);
    showRefreshButton(!mServerIsValid);
  }
}
