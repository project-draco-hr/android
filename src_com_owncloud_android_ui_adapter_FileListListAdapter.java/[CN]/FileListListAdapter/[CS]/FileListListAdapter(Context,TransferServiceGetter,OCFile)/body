{
  super(context,R.layout.list_item,file == null ? null : mStorageManager.getContent(file.getParentId()),cursorFrom,cursorTo,0);
  mContext=context;
  mAccount=AccountUtils.getCurrentOwnCloudAccount(mContext);
  mTransferServiceGetter=transferServiceGetter;
  mFile=file;
}
