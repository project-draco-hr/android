{
  OCFile file=getFile();
  if (file != null) {
    if (file.isDirectory()) {
      return file;
    }
 else {
      return mStorageManager.getFileById(file.getParentId());
    }
  }
 else {
    return null;
  }
}
