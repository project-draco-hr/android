{
  OCFile file=getFile();
  if (file != null) {
    if (file.isFolder()) {
      return file;
    }
 else     if (mStorageManager != null) {
      return mStorageManager.getFileById(file.getParentId());
    }
  }
  return null;
}
