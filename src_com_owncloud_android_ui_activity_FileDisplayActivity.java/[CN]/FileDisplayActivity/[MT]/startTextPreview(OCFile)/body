{
  Fragment textPreviewFragment=PreviewTextFragment.newInstance(file,getAccount());
  setSecondFragment(textPreviewFragment);
  updateFragmentsVisibility(true);
  updateActionBarTitleAndHomeButton(file);
  setFile(file);
}
