{
  if (file != null && FilePreviewFragment.canBePreviewed(file)) {
    if (file.isDown()) {
      startPreview(file);
    }
 else {
      startDownloadForPreview(file);
    }
  }
 else {
    startDetails(file);
  }
}
