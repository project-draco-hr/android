{
  Pair<String,String> putResult=mPendingOperations.putIfAbsent(account.name,remotePath,syncFolderOperation,null);
  if (putResult != null) {
    sendBroadcastNewSyncFolder(account,remotePath);
  }
}
