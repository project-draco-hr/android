{
  if (sslContext == null)   throw new IllegalArgumentException("AdvancedSslSocketFactory can not be created with a null SSLContext");
  mSslContext=sslContext;
  mHostnameVerifier=hostnameVerifier;
}
