{
  if (mHostnameVerifier != null) {
    try {
      mHostnameVerifier.verify(host,(SSLSocket)socket);
    }
 catch (    IOException iox) {
      try {
        socket.close();
      }
 catch (      Exception x) {
      }
      throw iox;
    }
  }
}
