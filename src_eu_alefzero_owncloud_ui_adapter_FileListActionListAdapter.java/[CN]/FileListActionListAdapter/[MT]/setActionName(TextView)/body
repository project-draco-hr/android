{
  if (mFileType.matches("image/.*")) {
    tv.setText("View");
  }
 else   if (mFileType.matches("audio/.*") || mFileType.matches("video/.*")) {
    tv.setText("Play");
  }
 else {
    tv.setText("Open");
  }
}
