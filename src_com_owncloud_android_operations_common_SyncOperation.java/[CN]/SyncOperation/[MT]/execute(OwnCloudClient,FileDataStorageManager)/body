{
  if (storageManager == null)   throw new IllegalArgumentException("Trying to execute a sync operation with a NULL storage manager");
  mStorageManager=storageManager;
  return super.execute(client);
}
