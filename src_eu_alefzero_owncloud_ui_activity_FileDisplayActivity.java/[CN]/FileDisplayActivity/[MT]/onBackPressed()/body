{
  popPath();
  if (mDirectories.getCount() == 0) {
    Intent intent=new Intent(this,LandingActivity.class);
    startActivity(intent);
    return;
  }
  ((FileListFragment)getSupportFragmentManager().findFragmentById(R.id.fileList)).onNavigateUp();
}
