{
  int result=getDB().delete(ProviderTableMeta.CONTENT_URI_UPLOADS,ProviderTableMeta._ID + "=?",new String[]{String.valueOf(uploadId)});
  Log_OC.d(TAG,"delete returns " + result + " for upload with ID "+ uploadId);
  if (result > 0) {
    notifyObserversNow();
  }
  return result;
}
